pipeline{
    agent any
    stages{
        stage('version'){
            steps{
                sh 'python --version'
            }
        }
        stage('pip install '){
            steps{
                sh 'pip install -r requirements.txt'
            }
        }
    }
}
